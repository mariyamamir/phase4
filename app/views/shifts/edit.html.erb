<h1>Editing Shift</h1>

<%= render 'form', shift: @shift %>

<%= button_to "Show", @shift, :class => "btn btn-primary", :method => :get %> <br>
<%= button_to "Shifts Home", shifts_path, :class => "btn btn-primary", :method => :get %>
