<h1>Editing Assignment</h1>

<%= render 'form', assignment: @assignment %>

<%= button_to "Show", @assignment, :class => "btn btn-primary", :method => :get %> <br>
<%= button_to "Assignments Home", assignments_path, :class => "btn btn-primary", :method => :get %>
