<h1>Editing Store</h1>

<%= render 'form', store: @store %>

<%= button_to "Show", @store, :class => "btn btn-primary", :method => :get %> <br>
<%= button_to "Stores Home", stores_path, :class => "btn btn-primary", :method => :get %>
